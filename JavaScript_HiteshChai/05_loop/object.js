const object={
    js:"javascript",
    opps:"object oriented programming",
    py:"python",
    cpp:"c++",
    rb:"ruby"
}
for(const key in object){
console.log(`${key } is short form is ${object[key]}`);
}